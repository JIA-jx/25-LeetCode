## [42. 接雨水 - 力扣（LeetCode）](https://leetcode.cn/problems/trapping-rain-water/description/)

![屏幕截图 2026-03-27 113736.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-27%20113736.png?raw=true)

```cpp
class Solution {
public:
    int trap(vector<int>& height) {
        // 单调栈：存储柱子下标，保持栈内对应柱子高度 单调递减
        stack<int> st;
        // 总接雨水量
        int sum = 0;

        // 初始化：将第一个柱子下标入栈
        st.push(0);
        // 从第二个柱子开始遍历
        for(int i = 1; i < height.size(); i++){
            // 核心：当前柱子 > 栈顶柱子 → 形成凹槽，可以接水
            while(!st.empty() && height[i] >= height[st.top()]){
                // 栈顶元素：凹槽的底部柱子下标
                int mid = st.top();
                st.pop(); // 弹出底部，准备计算
                
                // 栈不为空，说明有左边界，才能形成凹槽接水
                if(!st.empty()){
                    // 计算接水高度：左右边界的较小值 - 凹槽底部高度
                    int h = min(height[st.top()], height[i]) - height[mid];
                    // 计算接水宽度：右边界下标 - 左边界下标 - 1
                    int w = i - st.top() - 1;
                    // 累加当前凹槽的蓄水量
                    sum += h * w;
                }
            }
            // 栈保持单调递减，将当前柱子下标入栈
            st.push(i);
        }

        return sum;
    }
};
```

## [84. 柱状图中最大的矩形 - 力扣（LeetCode）](https://leetcode.cn/problems/largest-rectangle-in-histogram/description/)

![屏幕截图 2026-03-27 114514.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-27%20114514.png?raw=true)

```cpp
class Solution {
public:
    int largestRectangleArea(vector<int>& heights) {
        stack<int> st;  // 单调栈：存储柱子【下标】，维护栈内对应高度 单调递增
        int maxArea = 0; // 记录最大矩形面积（变量名更语义化，原sum功能一致）
        
        // 核心：首尾插入0！
        // 头部0：保证栈中所有原柱子都能被弹出计算（无左边界问题）
        // 尾部0：强制弹出栈内剩余所有柱子，完成最后计算（无右边界问题）
        heights.insert(heights.begin(), 0);
        heights.push_back(0);
        
        st.push(0); // 初始化：将第一个0的下标入栈
        // 从第二个元素开始遍历所有柱子
        for(int i = 1; i < heights.size(); i++){
            // 核心：当前柱子高度 < 栈顶柱子 → 找到右边界，开始计算面积
            while(!st.empty() && heights[i] < heights[st.top()]){
                int mid = st.top(); // 弹出的元素：矩形的高度对应的柱子
                st.pop();
                
                // 计算矩形宽度：右边界i - 左边界(新栈顶) - 1
                int width = i - st.top() - 1;
                // 计算面积，更新全局最大面积
                maxArea = max(maxArea, width * heights[mid]);
            }
            // 维持单调递增，将当前柱子下标入栈
            st.push(i);
        }

        return maxArea;
    }
};
```

