## [337. 打家劫舍 III](https://leetcode.cn/problems/house-robber-iii/submissions/714972749/)

![image-20260403222820817](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260403222820817.png)

```c
#define max(a, b) ((a) > (b) ? (a) : (b))

int *robTree(struct TreeNode *node) {
    int* amounts = (int*) malloc(sizeof(int) * 2);
    memset(amounts, 0, sizeof(int) * 2);
    if(node == NULL){
        return amounts;
    }
    int * left = robTree(node->left);
    int * right = robTree(node->right);
    // 偷当前节点
    amounts[1] = node->val + left[0] + right[0];
    // 不偷当前节点
    amounts[0] = max(left[0], left[1]) + max(right[0], right[1]);
    return amounts;
}

int rob(struct TreeNode* root) {
    int * dp = robTree(root);
    
    return max(dp[0], dp[1]);
}
```



## [121. 买卖股票的最佳时机](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/submissions/714972974/)

![image-20260403222924297](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260403222924297.png)

```c
#define max(a, b) ((a) > (b) ? (a) : (b))

int maxProfit(int* prices, int pricesSize){
    if(pricesSize == 0){
        return 0;
    }

    int ** dp = malloc(sizeof (int *) * pricesSize);
    for(int i = 0; i < pricesSize; i++){
        dp[i] = malloc(sizeof (int ) * 2);
    }

    dp[0][0] = -prices[0];
    dp[0][1] = 0;
    for(int i = 1; i < pricesSize; i++){
        dp[i][0] = max(dp[i - 1][0], - prices[i]);
        dp[i][1] = max(dp[i - 1][1], dp[i - 1][0] + prices[i]);
    }
    return dp[pricesSize - 1][1];
}
```

