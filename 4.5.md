## [226. 翻转二叉树](https://leetcode.cn/problems/invert-binary-tree/)

![image-20260405222029863](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260405222029863.png)

```c
struct TreeNode* invertTree(struct TreeNode* root){
    if(!root)
        return NULL;
        
    struct TreeNode* temp = root->right;
    root->right = root->left;
    root->left = temp;

    invertTree(root->left);
    invertTree(root->right);

    return root;
}
```



## [101. 对称二叉树](https://leetcode.cn/problems/symmetric-tree/)

![image-20260405222721197](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260405222721197.png)

```c
bool isSameTree(struct TreeNode* p, struct TreeNode* q) {
    if (p == NULL || q == NULL) {
        return p == q;
    }
    return p->val == q->val && isSameTree(p->left, q->right) && isSameTree(p->right, q->left);
}

bool isSymmetric(struct TreeNode* root) {
    return isSameTree(root->left, root->right);
}
```

