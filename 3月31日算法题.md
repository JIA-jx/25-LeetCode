## [122. 买卖股票的最佳时机 II - 力扣（LeetCode）](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-ii/description/)

![屏幕截图 2026-03-31 212823.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-31%20212823.png?raw=true)

```cpp
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        // dppre：前一天状态 [0]持有股票 [1]不持有
        vector<int> dppre(2, 0);
        vector<int> dpcur(2, 0);

        // 初始：首日买入股票
        dppre[0] = -prices[0];

        for(int i = 1; i < prices.size(); i++){
            // 当天持有：保持原有 / 前一天不持有+今日买入
            dpcur[0] = max(dppre[0], dppre[1] - prices[i]);
            // 当天不持有：保持原有 / 前一天持有+今日卖出
            dpcur[1] = max(dppre[1], dppre[0] + prices[i]);
            // 滚动更新状态
            dppre = dpcur;
        }
        // 最终最大利润：不持有股票
        return dppre[1];
    }
};
```

## [123. 买卖股票的最佳时机 III - 力扣（LeetCode）](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/description/)

![屏幕截图 2026-03-31 212831.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-31%20212831.png?raw=true)

```cpp
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        // 5种状态：0无操作 1第一次买入 2第一次卖出 3第二次买入 4第二次卖出
        vector<vector<int>> dp(prices.size(), vector<int>(5, 0));

        // 初始状态：第0天两次买入
        dp[0][1] = -prices[0];
        dp[0][3] = -prices[0];
        
        for(int i = 1; i < prices.size(); i++){
            dp[i][0] = dp[i-1][0]; // 无操作，保持不变
            dp[i][1] = max(dp[i-1][1], -prices[i]); // 第一次买入
            dp[i][2] = max(dp[i-1][2], dp[i-1][1] + prices[i]); // 第一次卖出
            dp[i][3] = max(dp[i-1][3], dp[i-1][2] - prices[i]); // 第二次买入
            dp[i][4] = max(dp[i-1][4], dp[i-1][3] + prices[i]); // 第二次卖出
        }

        // 最大利润：完成两次卖出
        return dp[prices.size()-1][4];
    }
};
```

