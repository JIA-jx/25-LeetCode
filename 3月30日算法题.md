## [337. 打家劫舍 III - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber-iii/)

![屏幕截图 2026-03-30 210327.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-30%20210327.png?raw=true)

```cpp
class Solution {
public:
    int rob(TreeNode* root) {
        vector<int> dp(2, 0);
        // 递归获取根节点选/不选的最大金额
        dp = dfs(root);
        return max(dp[0], dp[1]);
    }
private:
    // 返回值：dp[0]不偷当前节点，dp[1]偷当前节点
    vector<int> dfs(TreeNode* root){
        vector<int> dp(2, 0);
        vector<int> dpl(2, 0);
        vector<int> dpr(2, 0);
        // 空节点返回0
        if(!root){
            return dp;
        }
        // 递归左右子树
        dpl = dfs(root->left);
        dpr = dfs(root->right);
        // 不偷当前节点：左右子树可偷可不偷，取最大
        dp[0] = max(dpl[1], dpl[0]) + max(dpr[0], dpr[1]);
        // 偷当前节点：左右子树均不能偷
        dp[1] = dpl[0] + dpr[0] + root->val;

        return dp;
    }
};
```



## [121. 买卖股票的最佳时机 - 力扣（LeetCode）](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/)

![屏幕截图 2026-03-30 210538.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-30%20210538.png?raw=true)

```cpp
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        vector<int> dp(2, 0);
        // dp[0]：持有股票的最大利润，初始买入首日股票
        dp[0] = -prices[0];
        // dp[1]：不持有股票的最大利润，初始为0
        dp[1] = 0;

        for(int i = 1; i < prices.size(); i++){
            // 更新持有状态：保持原有/今日买入
            dp[0] = max(dp[0], -prices[i]);
            // 更新不持有状态：保持原有/今日卖出
            dp[1] = max(dp[1], prices[i] + dp[0]);
        }

        return dp[1];
    }
};
```

