## [300. 最长递增子序列 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-increasing-subsequence/description/)

![屏幕截图 2026-03-24 211601.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-24%20211601.png?raw=true)

```cpp
class Solution {
public:
    int lengthOfLIS(vector<int>& nums) {
        int n = nums.size();
        // dp[i] 定义：以 nums[i] 结尾的最长递增子序列的长度
        // 初始化：每个元素自身都是长度为1的子序列
        vector<int> dp(n, 1);
        // 记录全局最长递增子序列的长度，初始最小为1
        int maxLen = 1;
        
        // 外层循环：遍历每个元素，计算以当前元素结尾的LIS长度
        for(int i = 1; i < n; i++){
            // 内层循环：遍历当前元素之前的所有元素，寻找可拼接的递增子序列
            for(int j = i-1; j >= 0; j--){
                // 核心：如果前面元素 < 当前元素，说明可以拼接
                if(nums[j] < nums[i]){
                    // 状态转移方程：更新dp[i]为最大值
                    dp[i] = max(dp[i], dp[j] + 1);
                }
            }
            // 每次计算完dp[i]，更新全局最长长度
            maxLen = max(dp[i], maxLen);
        }
        // 返回最终结果
        return maxLen;
    }
};
```

## [674. 最长连续递增序列 - 力扣（LeetCode）](https://leetcode.cn/problems/longest-continuous-increasing-subsequence/description/)

![屏幕截图 2026-03-24 211601.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-24%20211601.png?raw=true)

```cpp
class Solution {
public:
    
    int findLengthOfLCIS(vector<int>& nums) {
        // pre：记录以 当前元素 结尾的 连续递增子序列长度
        // 初始化：单个元素自身长度为1
        int pre = 1;
        // len：记录全局最长的连续递增子序列长度
        int len = 1;
        
        // 从第二个元素开始遍历，依次和前一个元素比较
        for(int i = 1; i < nums.size(); i++){
            // 核心条件：当前元素 > 前一个元素 → 连续递增，长度+1
            if(nums[i] > nums[i-1]){
                int cur = pre + 1;
                pre = cur;
            }
            // 不满足连续递增 → 重置当前连续长度为1
            else{
                pre = 1;
            }
            // 每次更新全局最大长度
            len = max(len, pre);
        }
        return len;
    }
};
```

