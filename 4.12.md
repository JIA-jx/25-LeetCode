## [239. 滑动窗口最大值 - 力扣（LeetCode）](https://leetcode.cn/problems/sliding-window-maximum/)

![image-20260412202627870](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260412202627870.png)

```c
int* maxSlidingWindow(int* nums, int numsSize, int k, int* returnSize) {
    *returnSize = numsSize - k + 1;
    int* res = (int*)malloc(sizeof(int) * (*returnSize));
    int resTop = 0;
    int* queue = (int*)malloc(sizeof(int) * numsSize);
    int front = 0, rear = 0;

    for(int i = 0; i < numsSize; i++) {
        if(front < rear && queue[front] <= i - k) {
            front++;
        }
        while(front < rear && nums[queue[rear - 1]] <= nums[i]) {
            rear--;
        }
        queue[rear++] = i;
        if(i >= k - 1) {
            res[resTop++] = nums[queue[front]];
        }
    }
    return res;
}
```



## [142. 环形链表 II - 力扣（LeetCode）](https://leetcode.cn/problems/linked-list-cycle-ii/)

![image-20260412223403724](C:\Users\OMEN\AppData\Roaming\Typora\typora-user-images\image-20260412223403724.png)

```c
struct ListNode *detectCycle(struct ListNode *head) {
    struct ListNode* fast = head;
    struct ListNode* slow = head;

    while(fast && fast->next) {
        fast = fast->next->next;
        slow = slow->next;
        if(fast == slow) {
            struct ListNode* ptr = head;
            while(fast != ptr) {
                ptr = ptr->next;
                fast = fast->next;
            }
            return fast;
        }
    }
    return NULL;
}
```

