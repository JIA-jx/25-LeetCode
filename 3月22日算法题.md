## [583. 两个字符串的删除操作 - 力扣（LeetCode）](https://leetcode.cn/problems/delete-operation-for-two-strings/)

![屏幕截图 2026-03-22 174134.png](https://github.com/zhanglihang666/leetcodeImage/blob/main/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-22%20174134.png?raw=true)

```cpp
class Solution {
public:
    int minDistance(string word1, string word2) {
        // dp[i][j]：word1前i个字符 和 word2前j个字符 相同的最小删除步数
        vector<vector<int>> dp(word1.size()+1, vector<int>(word2.size()+1, 0));
        
        //word2为空，删除word1前i个字符需要i步
        for(int i = 1; i <= word1.size(); i++) dp[i][0] = i;
        
       //word1为空，删除word2前j个字符需要j步
        for(int i = 1; i <= word2.size(); i++) dp[0][i] = i;
        
        
        for(int i = 1; i <= word1.size(); i++){
            for(int j = 1; j <= word2.size(); j++){
                // 字符相同，无需删除，继承子问题的结果
                if(word1[i-1] == word2[j-1]){
                    dp[i][j] = dp[i-1][j-1];
                }
                else{
                    // 字符不同：选「删word1当前字符」或「删word2当前字符」中步数更少的
                    // dp[i-1][j]+1：删word1的第i个字符（i-1是字符串下标），步数+1
                    // dp[i][j-1]+1：删word2的第j个字符，步数+1
                    dp[i][j] = min(dp[i - 1][j] + 1, dp[i][j - 1] + 1);
                }
            }
        }

        return dp[word1.size()][word2.size()];
    }
};
```

## [72. 编辑距离 - 力扣（LeetCode）](https://leetcode.cn/problems/edit-distance/description/)

![%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-22%20172441.png](https://raw.githubusercontent.com/zhanglihang666/leetcodeImage/812398f957ca209909db5245dc3bfafdade618f4/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202026-03-22%20172441.png)

```cpp
class Solution {
public:
    int minDistance(string word1, string word2) {
        
        // 定义dp二维数组，dp[i][j]表示将word1前i个字符转为word2前j个字符的最小操作数
        vector<vector<int>> dp(word1.size()+1, vector<int>(word2.size()+1, 0));
        
        // 初始化边界：word1前i个字符转为空字符串（word2前0个字符），需要删除i次
        for(int i = 1; i <= word1.size(); i++) dp[i][0] = i;
        
        // 初始化边界：空字符串（word1前0个字符）转为word2前i个字符，需要插入i次
        for(int i = 1; i <= word2.size(); i++) dp[0][i] = i;
        
        for(int i = 1; i <= word1.size(); i++){   
            for(int j = 1; j <= word2.size(); j++){
                
                // 若当前字符相等
                if(word1[i-1] == word2[j-1]){
                    // 无需操作，继承word1前i-1个转word2前j-1个的最小操作数
                    dp[i][j] = dp[i-1][j-1];
                }
                else{
                    // 字符不等时，取三种操作的最小值+1（每种操作算1步）：
                    // dp[i-1][j]：删除word1当前字符；dp[i][j-1]：插入字符到word1；dp[i-1][j-1]：替换word1当前字符
                    dp[i][j] = min({dp[i-1][j], dp[i][j-1], dp[i-1][j-1]})+1;
                }
            }
        }
        return dp[word1.size()][word2.size()];
    }
};
```



