## [240. 搜索二维矩阵 II - 力扣（LeetCode）](https://leetcode.cn/problems/search-a-2d-matrix-ii/submissions/709488667/?envType=study-plan-v2&envId=top-100-liked)[3070. 元素和小于等于 k 的子矩阵的数目 - 力扣（LeetCode）](https://leetcode.cn/problems/count-submatrices-with-top-left-element-and-sum-less-than-k/description/?envType=daily-question&envId=2026-03-18)

![](https://raw.githubusercontent.com/ningmeng2134/git-demo/main/3.19-1-2026-03-19-21%3A58%3A13.png)

```cpp
class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        int i = matrix.size() - 1, j = 0;//从右上角遍历，将数组45度倾斜，类似于二叉搜索时
        while( i >= 0 && j < matrix[0].size()){
            if(matrix[i][j] > target) i--;//比中间值小向左移
            else if(matrix[i][j] < target) j++;//比中间值大向右移
            else return true;
        }
        return false;
    }
};
```

## [198. 打家劫舍 - 力扣（LeetCode）](https://leetcode.cn/problems/house-robber/submissions/709493916/?envType=study-plan-v2&envId=top-100-liked)

![](https://raw.githubusercontent.com/ningmeng2134/git-demo/main/3.19-2-2026-03-19-22%3A07%3A04.png)

```cpp
class Solution {
public:
    int rob(vector<int>& nums) {
        int n = nums.size();//房间数
        vector<int> dp(n);//创建dp，记录第i个能偷到的最大值
        if (n == 0) return 0;//没有房间偷
        if (n == 1) return nums[0];//只有一个房间偷
        if (n == 2) return max(nums[0], nums[1]);//两个房间选一个偷
        dp[0] = nums[0];//第一个房间金额为本身
        dp[1] = max(nums[0], nums[1]);//前两个房间偷最大利益
        for(int i = 2; i < n; i++){
            dp[i] = max(dp[i - 1], dp[i - 2] + nums[i]);//选择不偷当前房间或者偷当前房间的最大利益
        }
        return dp[n - 1];
    }
};
```

